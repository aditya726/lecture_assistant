fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
pymongo==4.6.1
pydantic-settings==2.1.0
python-dotenv==1.0.0
ollama==0.1.6
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
httpx==0.26.0
pydantic[email]==2.5.3

# Crypto used directly in app/core/security.py (passlib extra usually pulls it in, but keep explicit)
bcrypt==4.1.2

# Embeddings + reranking
sentence-transformers==2.5.1

# Vector search + BM25
numpy==1.26.4
rank-bm25==0.2.2
# NOTE: FAISS wheels are often unavailable on Windows via pip.
# - On Windows, prefer: conda install -c conda-forge faiss-cpu
# - On Linux/macOS, pip typically works:
faiss-cpu==1.7.4; platform_system != "Windows"

# YouTube Data API client (googleapiclient)
google-api-python-client==2.118.0

faster-whisper
torch==2.1.2
torchaudio==2.1.2
Pillow==10.2.0
pdf2image==1.17.0
PyPDF2==3.0.1
python-docx==1.1.0
aiofiles==23.2.1

rapidocr-onnxruntime

# OpenCV 4.6 is too old for Python 3.11 wheels; bump to a cp311-compatible build.
opencv-python==4.9.0.80
scikit-image==0.22.0
shapely==2.0.2
pyclipper==1.3.0.post5
rapidfuzz==3.6.1
imgaug==0.4.0
lmdb==1.4.1
